import React from 'react';
import ReactDOM from 'react-dom/client';
import PetDiaryApp from './PetDiaryApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PetDiaryApp />
  </React.StrictMode>
);
